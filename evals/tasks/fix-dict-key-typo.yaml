id: fix-dict-key-typo
prompt: "get_timeout() in settings.py raises a KeyError because it looks up a typo'd dict key. Fix the typo so it reads the correct key from CONFIG."
fixture: fix-dict-key-typo
timeout_s: 300
checks:
  - type: command
    run: "python -m pytest test_settings.py -q"
